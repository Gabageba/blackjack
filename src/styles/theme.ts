import { css } from '@emotion/react';

export const colors = {
  white: '#fff',
} as const;

export const contentCenter = css`
  display: flex;
  align-items: center;
  justify-content: center;
`;
